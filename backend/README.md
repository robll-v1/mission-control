# Backend

FastAPI service for PR review task creation, review round execution, event streaming, artifact export, and the built-in static console entry.

## Run
- From the repository root: `./scripts/dev-backend.sh`

## Health
- `http://127.0.0.1:8000/api/health`
